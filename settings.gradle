plugins {
    id "com.gradle.enterprise" version "3.5.1"
}

if (System.getenv("CI") != null) gradleEnterprise{
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
    }
}

rootProject.name = 'YJump'
